instance.superField(name, value);

List markenList = init.get("markenList");
if("column1".equals(name) && markenList.contains(value)) {
    instance.superField("myFlag", "true");
} else {
    instance.superField("myFlag", "false");
}